{
  "version": 1,
  "rules": [
    {
      "pattern": "(^|/)[^/]*\\.env($|\\.)",
      "reason": "Environment files may contain credentials."
    },
    {
      "pattern": "(^|/)(credentials?|secrets?)(/|$)",
      "reason": "Credential and secret files or directories must not be committed."
    },
    {
      "pattern": "\\.(pem|key|p12|pkcs12|jks|pfx)$",
      "reason": "Private key and certificate files must not be committed."
    },
    {
      "pattern": "(^|/)id_(rsa|ed25519)($|\\.)",
      "reason": "SSH private key files must not be committed."
    },
    {
      "pattern": "(^|/)\\.(aws|gcp)(/|$)",
      "reason": "Cloud credential directories must not be committed."
    },
    {
      "pattern": "(^|/)(credentials?|secrets?|client[_-]?secret|service[-_]?account[^/]*)\\.(json|ya?ml|toml|ini|conf|config|txt)$",
      "reason": "Application credential files must not be committed."
    }
  ]
}
