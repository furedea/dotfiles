---
name: tsdd
description: >
    Test-Spec Driven Development (TSDD): tests are the durable specification, TDD is the operating discipline, prose carries only Why, and `CLAUDE.md` stays a navigation map. Load before production code, tests, ADRs, architectural decisions, `CLAUDE.md` edits, or any durable prose artifact that would duplicate tests or code. Also triggers on "TSDD", "Test-Spec Driven Development", "TDD で実装", "ADR を書いて", "decision record", or "executable specification".
---

Read `INSTRUCTIONS.md` (in this skill's directory) for the full TSDD methodology before proceeding. The ordering of layers (What → Tests, How → Code + Types, Why → ADR + Comments, Navigation → `CLAUDE.md`) and the TDD cycle discipline are the load-bearing parts — do not skim.
