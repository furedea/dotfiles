export PATH=$HOME/.gem/bin:$HOME/.cargo/bin:$PATH
# Enforced on every zsh start so stale inherited environments
# (e.g. a long-running herdr server snapshot) cannot override it.
export EDITOR=nvim
export VISUAL=nvim
